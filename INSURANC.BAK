#include<stdio.h>
#include<conio.h>
void main()
{ int age,gender,smoke,amount=0;
 clrscr();
 printf("enter the AGE");
 scanf("%d",&age);
 printf("enter the gender 0=male or 1=female");
 scanf("%d",&gender);
 printf("enter person do smoke or not 0=no or 1=yes");
 scanf("%d",&smoke);
 if(21<=age<30)
 { amount=10000;
   if(gender==0)
    { amount=amount-((amount*10)/100);
      if(smoke==1)
      { amount=amount+((amount*10)/100);
       printf("amount of insurance is %d",amount);
       }
      else
      printf("amount of insurance is %d",amount);
    }
    else
   { if(smoke==1)
    {amount=amount+((amount*10)/100);
    printf("amount of insurance is%d",amount);
   }
   else
   printf("amount of insurance is%d",amount);
   }}
 if(31<=age<40)
 { amount=15000;
  if(gender==0)
  { amount=amount-((amount*10)/100);
    if(smoke==1)
    {amount=amount+((amount*10)/100);
    printf("amount ofinsurance is%d",amount);
    }
    else
    printf("amount of insurance is%d",amount);
    }
    else
    { if(smoke==1)
     {amount=amount+((amount*10)/100);
     printf("amount of insurance is%d",amount);
     }
     else
     printf("amount ofninsurance is%d",amount);
     }
     }
  if(41<=age<50)
  { amount=20000;
   if(gender==0)
    { amount=amount-((amount*10)/100);
      if(smoke==1)
      {  amount=amount+((amount*10)/100);
	printf("amount of insurance is%d",amount);
	}
     else
     printf("amount of insurance is %d",amount);
     }
     else
     { if(smoke==1)
       {amount=amount+((amount*10)/100);
       printf("amount of insurance is%d",amount);
       }
     else
     printf("amount of insurance is%d",amount);
     }}
  if(51<=age<60)
  { amount=40000;
   if(gender==0)
    {amount=amount-((amount*10)/100);
     if(smoke==1)
     { amount=amount+((amount*10)/100);
      printf("amount of insurance is%d",amount);
      }
     else
     printf("amount of insurance is%d",amount);
     }
    else
    { if(smoke==1)
     { amount=amount+((amount*10)/100);
      printf("amount of insurance is%d",amount);
      }
    else
    printf("amount of insurance is%d",amount);
    }
  }
 else
 printf("insurance is not possible");
 getch();
 }



